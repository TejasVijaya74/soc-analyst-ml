services:

  backend:
    build:
      context: ./backend
    container_name: soc-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: soc-frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend